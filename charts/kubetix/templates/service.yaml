apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubetix.fullname" . }}
  labels:
    {{- include "kubetix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubetix.selectorLabels" . | nindent 4 }}
